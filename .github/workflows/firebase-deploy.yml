name: Firebase Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.0.0
      
      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Install Dependencies and Build
        run: |
          npm ci
          npm run build

      - name: Firebase Deploy
        run: |
          npx firebase use meme-talk-4adb4
          npx firebase deploy --only hosting

      - name: Debug
        run: cat /home/runner/.npm/_logs/2024-02-03T12_54_50_236Z-debug-0.log
        env:
          FIREBASE_TOKEN: 1//0e_Ww3fM0uoeUCgYIARAAGA4SNwF-L9IrRuPaKNHsevuDoGcvhgrWtHjm-CzUdwfXl231Jmm2vEjiGMWKrQsmNFR1C2YTCYoZgA4
        shell: /bin/bash -e {0}